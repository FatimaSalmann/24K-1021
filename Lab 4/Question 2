#include<stdio.h>
int main()
{
int number;
printf("enter a number");
scanf("%d", &number);
if
(number>=0 && number%2==0 && number%10==0)
{printf("Number is valid\n");}
else 
{printf("number is not valid\n");}
return 0;
}
