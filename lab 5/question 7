#include <stdio.h>
int main() {
    int number;
    printf("Enter a number: ");
    scanf("%d", &number);
    (number % 10 == 0) ?
	printf("Last Digit is zero\n") 
	: printf("Last Digit is non-zero\n");
 return 0;
}


// if = ?
// else = :
